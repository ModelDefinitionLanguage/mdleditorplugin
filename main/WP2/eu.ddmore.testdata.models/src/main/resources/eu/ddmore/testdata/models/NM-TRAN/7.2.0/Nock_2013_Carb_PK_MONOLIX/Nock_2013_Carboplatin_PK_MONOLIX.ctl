; Script generated by the pharmML2Nmtran Converter v.1.0.0
; Source	: PharmML 0.3.1
; Target	: NMTRAN 7.2.0
; Model 	: Nock_2013_Carboplatin_PK_MONOLIX
; Dated 	: Wed Feb 11 14:01:39 GMT 2015

$PROBLEM "Nock_2013_Carboplatin_PK_MONOLIX - generated by MDL2PharmML v.6.0"

$INPUT ID TIME TALD AMT DUR RATE DV EVID DOSE AGE HGT KG BSA CLCR CREA TRT LOGTCLCR LOGTKG
$DATA Carbo_DDMoRe_log2.csv IGNORE=#
$SUBS ADVAN13 TOL=9

$MODEL
COMP (AC)
COMP (AP)

$PK
THCL = THETA(1)
THV1 = THETA(2)
THQ = THETA(3)
THV2 = THETA(4)
SDADD = THETA(5)
SDPROP = THETA(6)
CLCLCR_COV = THETA(7)
V1KG_COV = THETA(8)

eta_OMCL = ETA(1)
eta_OMV1 = ETA(2)

MU_1 = LOG(THCL)+CLCLCR_COV * logtCLCR;
CL = EXP(MU_1 + ETA(1));

MU_2 = LOG(THV1)+V1KG_COV * logtKG;
V1 = EXP(MU_2 + ETA(2));


Q =  THQ ;


V2 =  THV2 ;

A_0(1) = 0
A_0(2) = 0

$DES
AC = A(1)
AP = A(2)

IPRED_DES =  (AC/V1)
DADT(1) = (((-((Q/V1))*AC)-((CL/V1)*AC))+((Q/V2)*AP))
DADT(2) = (((Q/V1)*AC)-((Q/V2)*AP))

$ERROR
IPRED =  (A(1)/V1)

IPRED = IPRED
W = SQRT((SDADD*SDADD)+ (SDPROP*SDPROP*IPRED*IPRED))
Y = IPRED+W*EPS(1)
IRES = DV - IPRED
IWRES = IRES/W

$THETA
( 0.0 , 8.3 )	;THCL
( 0.0 , 22.0 )	;THV1
( 0.0 , 0.76 )	;THQ
( 0.0 , 31.0 )	;THV2
( 0.0 , 0.015 )	;SDADD
( 0.0 , 0.2 )	;SDPROP
( -10.0 , 0.69 , 10.0 )	;CLCLCR_COV
( -10.0 , 0.69 , 10.0 )	;V1KG_COV

$OMEGA
( 0.019 )	;OMCL
( 0.0  FIX )	;OMV1

$SIGMA
1.0 FIX	;SIGMA


$EST METHOD=COND INTER MAXEVALS=9999 PRINT=10 NOABORT
$COV

$TABLE ID TIME TALD AMT DUR RATE EVID DOSE AGE HGT KG BSA CLCR CREA TRT LOGTCLCR LOGTKG PRED IPRED RES IRES WRES IWRES Y DV NOAPPEND NOPRINT FILE=sdtab

$TABLE ID CL V1 Q V2 ETA_OMCL ETA_OMV1 NOAPPEND NOPRINT FILE=patab

$TABLE ID AGE HGT KG BSA CLCR CREA LOGTCLCR LOGTKG NOAPPEND NOPRINT FILE=cotab


