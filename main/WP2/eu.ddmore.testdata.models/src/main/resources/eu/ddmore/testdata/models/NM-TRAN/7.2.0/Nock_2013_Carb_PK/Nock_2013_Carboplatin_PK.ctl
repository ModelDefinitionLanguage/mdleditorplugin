; Script generated by the pharmML2Nmtran Converter v.1.0.0
; Source	: PharmML 0.3.1
; Target	: NMTRAN 7.2.0
; Model 	: Nock_2013_Carboplatin_PK
; Dated 	: Wed Feb 11 14:01:37 GMT 2015

$PROBLEM "Nock_2013_Carboplatin_PK - generated by MDL2PharmML v.6.0"

$INPUT  ID TIME TALD AMT DUR RATE DV EVID DOSE AGE HGT KG BSA CLCR CREA TRT LOGTCLCR
$DATA Carbo_DDMoRe_log.csv IGNORE=#
$SUBS ADVAN13 TOL=9

$MODEL
COMP (AC)
COMP (AP)

$PK
THCL = THETA(1)
THV1 = THETA(2)
THQ = THETA(3)
THV2 = THETA(4)
SDADD = THETA(5)
SDPROP = THETA(6)
CLCLCR_COV = THETA(7)
V1KG_COV = THETA(8)

eta_OMCL = ETA(1)

MU_1 = LOG(THCL)+CLCLCR_COV * logtCLCR;
CL = EXP(MU_1 + ETA(1));


V1 =  TVV1 ;


Q =  THQ ;


V2 =  THV2 ;


VSS =  (V1+V2) ;


K =  (CL/V1) ;


K12 =  (Q/V1) ;


K21 =  (Q/V2) ;

A_0(1) = 0
A_0(2) = 0

$DES
AC = A(1)
AP = A(2)

IPRED_DES =  (AC/V1)
DADT(1) = (((-(K12)*AC)-(K*AC))+(K21*AP))
DADT(2) = ((K12*AC)-(K21*AP))

$ERROR
IPRED =  (A(1)/V1)

IPRED = IPRED
W = SQRT((SDADD*SDADD)+ (SDPROP*SDPROP*IPRED*IPRED))
Y = IPRED+W*EPS(1)
IRES = DV - IPRED
IWRES = IRES/W

$THETA
( 0.0 , 8.3 )	;THCL
( 0.0 , 22.0 )	;THV1
( 0.0 , 0.76 )	;THQ
( 0.0 , 31.0 )	;THV2
( 0.0 , 0.015 )	;SDADD
( 0.0 , 0.2 )	;SDPROP
( -10.0 , 0.69 , 10.0 )	;CLCLCR_COV
( -10.0 , 0.69 , 10.0 )	;V1KG_COV

$OMEGA
( 0.019 )	;OMCL

$SIGMA
1.0 FIX	;SIGMA


$EST METHOD=COND MAXEVALS=9999 PRINT=10 NOABORT
$COV

$TABLE  ID TIME TALD AMT DUR RATE EVID DOSE AGE HGT KG BSA CLCR CREA TRT LOGTCLCR PRED IPRED RES IRES WRES IWRES Y DV NOAPPEND NOPRINT FILE=sdtab

$TABLE  ID CL V1 Q V2 VSS K K12 K21 ETA_OMCL NOAPPEND NOPRINT FILE=patab

$TABLE  ID KG CLCR LOGTCLCR NOAPPEND NOPRINT FILE=cotab


